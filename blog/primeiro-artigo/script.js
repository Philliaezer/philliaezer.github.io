// Não vai ter nada de interessante por enquanto 
const patti = /oi/;

/*

url = "/path/to/file.md?nonce=" + Date.now().toString(); // added nonce to prevent caching, you can remove it if you want the files to cache
$.ajax({
  url: url,
  type: "GET",
  dataType: "text",
  success: function (results) {
    var html = self.converter.makeHtml(results);
    // do something with the html here
});

*/